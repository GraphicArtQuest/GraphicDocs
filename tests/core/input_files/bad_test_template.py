"""Template has no `build` method, so it should fail to load and throw the `error_loading_template` action hook."""

test_value = "Test"   # Used with the `unittest` module to check it loaded properly
